#data specification for site rendering nomenclature
init_user_page_summary <- data.frame(
  id = NA_character_,
  compliance_page = NA_character_,
  compliance_render_date = as.POSIXct(NA),
  physio_page = NA_character_,
  physio_render_date = as.POSIXct(NA),
  video_page = NA_character_,
  video_render_date = as.POSIXct(NA)
)
